---
title: "SharePoint for Fieldwork"
description: "Keep engagement documents in your organization's own SharePoint, synced to the engagement lifecycle"
---

<Info>
  Recap IA's SharePoint integration is non-essential, and an optional client configuration. It provides an option for users to allow Recap IA to interoperate with a live-document store hosted in their organization's own environment.
</Info>

<video src="/videos/Screen-Recording-2026-08-11-at-9.44.21-AM.mp4" controls />

## How it works

Recap IA is the system of record for **cases and workflow**; SharePoint is the **document store**. The integration keeps the two in sync so auditors never hand-manage folders: as cases move through their lifecycle, folders and files are created automatically in your organization's SharePoint site, and each created item's ID is stored back on the case.

## Folder structure

When an Engagement is created, Recap IA creates a folder for it in the document library, along with `Action Items` and `Notes` subfolders. When each Test is created within the engagement, Recap IA creates a folder for that test and copies a blank workpaper template into it.

<Tree>
  <Tree.Folder name="{Engagement ID} {Engagement Name}" defaultOpen>
    <Tree.Folder name="Action Items" />
    <Tree.Folder name="Notes" />
    <Tree.Folder name="{Test Name}" defaultOpen>
      <Tree.File name="{Test Name} Workpaper.xlsx" />
    </Tree.Folder>
  </Tree.Folder>
</Tree>

| Event in Recap IA | What happens in SharePoint |
| --- | --- |
| Engagement is created | Engagement folder created, with `Action Items` and `Notes` subfolders |
| Test is created | Test folder created inside the engagement folder; blank workpaper template copied in as `{Test Name} Workpaper.xlsx` |
| Fieldwork | Assigned auditors get **edit** access to their workpaper and open it directly in Excel from the test case |
| Test submitted for review | Auditors' edit access is revoked; the reviewer receives a **view \+ comment** link |

## Access control

- **Auditors** assigned to a test get edit access to that test's workpaper during fieldwork.
- **Reviewers** (the engagement's Audit Manager) receive a review link — view and comment, but not edit — once the workpaper is submitted.
- Sharing is managed by Recap IA through Microsoft Graph; no one on the audit team needs to manage SharePoint permissions by hand.

## Setup requirements

The integration connects through Microsoft Graph using an app registration in your organization's Entra ID tenant, granted `Sites.Selected` access (read and write) to the specific SharePoint site used for audit documents. This means Recap IA can only reach the site you designate — not the rest of your SharePoint environment.